export default function App() {
  return (
    <main className="shell">
      <p className="eyebrow">Playground</p>
      <h1>Say what to build</h1>
      <p className="lede">
        Speak a feature. Watch this pane become the app. Then drive it by voice — click, type,
        scroll — no hands required.
      </p>
    </main>
  );
}
