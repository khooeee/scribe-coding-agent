# Playground

Live webapp the voice coding agent edits.

**Do not remove** `import "./voice-bridge"` from `src/main.tsx`. That bridge powers voice click / type / scroll / key tools from the Electron shell.
