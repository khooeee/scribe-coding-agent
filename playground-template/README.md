# Playground template

Source template for **File → New Project**.

`New Project` copies this folder to `../playground/` (gitignored). The voice coding agent edits `playground/`, never this template.

**Do not remove** `import "./voice-bridge"` from `src/main.tsx`.
